{
  "name": "@cropcard/plugin-validation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Plugin Zod schemas + bypass check shared between CropCard (apps/web) and the marketplace (apps/marketplace). Safety-kernel CHEMISTRY_KILL_MATRIX is consumed via a generated snapshot — the canonical source stays in apps/web/src/lib/safety/ per CLAUDE.md invariant 1.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./bypass": "./src/bypassCheck.ts",
    "./schemas": "./src/schemas.ts",
    "./safety-snapshot": "./src/safetySnapshot.ts"
  },
  "scripts": {
    "gen:safety-snapshot": "tsx scripts/gen-snapshot.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.22.0",
    "typescript": "^5.5.4"
  }
}

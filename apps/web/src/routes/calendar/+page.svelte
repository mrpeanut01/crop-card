<!--
  Stub component — the +page.server.ts load() throws redirect(302) before
  this template ever renders. Kept here because SvelteKit wants a +page
  for the route to register cleanly with `dev` HMR.
-->
<p>Redirecting…</p>
